# GitHub Manager 2.0.79

Versão: `2.0.79+200093`

Release corretiva baseada nos logs Android-APK-69 e CI-69. Remove o uso incompatível de `AsyncValue.valueOrNull` na tela global de Builds, preserva o polling adaptativo com `AsyncData`, confirma o retorno `ManagedUploadBuildPolicy` do diálogo de envio e adiciona teste de regressão para esse contrato. Também limpa warnings introduzidos pela reorganização recente da tela de Projetos.
